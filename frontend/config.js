// EduPredict - Frontend Config
// After you deploy the backend on Render, paste its URL below.
// Example: "https://edupredict-backend.onrender.com"
// Leave "" (empty) only for local testing when frontend & backend run on the same origin.
window.API_BASE_URL = "http://127.0.0.1:5000";
